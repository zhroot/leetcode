function bfs(x,y,close){
    let curMat = 
    //上
    //下
    //左
    //右

}
/**
 * @param {number[][]} mat
 * @return {number}
 */
var minFlips = function(mat) {
    const open = [];
    const close = {};
    const M = mat.length;
    const N = mat[0].length;
    while(open.length>0){
        
    }
    for(let i = 0;i<M;i++){
        for(let j = 0;j<N;j++){

        }
    }
};